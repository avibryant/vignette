#!/usr/bin/env ruby
$:.unshift(File.dirname(__FILE__))
require 'lib/rumour'

include Rumour

address = ARGV.shift
seed = ARGV.shift

daemon = Daemon.new(address)
daemon.bootstrap(seed)
daemon.start
loop do
  n = rand(1000)
  daemon.update("foo", {0=>n})
  sleep(1)
end